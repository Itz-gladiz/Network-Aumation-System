Django>=5.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.2
cryptography>=42.0
python-decouple>=3.8
psycopg2-binary>=2.9   # only needed if you switch DB_ENGINE to postgresql
netmiko>=4.3
gitpython>=3.1          # only needed once backups commit to Git
celery>=5.4              # only needed for async/bulk jobs
redis>=5.0                # celery broker
